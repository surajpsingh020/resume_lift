# Copy to .env for local development and fill values. Do NOT commit secrets.

# Backend
PORT=5001
MONGODB_URI=mongodb://localhost:27017/ai_resume_builder
JWT_SECRET=your_jwt_secret_here

# Gemini (move this to server-side and set in hosting provider for production)
GEMINI_API_KEY=your_gemini_api_key_here

# Any other secrets used by the backend
# EXAMPLE: SENTRY_DSN=...
